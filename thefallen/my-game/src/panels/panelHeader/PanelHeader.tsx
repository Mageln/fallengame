import { Panel } from '@vkontakte/vkui'
import React from 'react'

export const PanelHeader = () => {
  return (
    <Panel id={id}>
        <PanelHeader>

        </PanelHeader>
    </Panel>
  )
}
